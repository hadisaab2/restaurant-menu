/**
 * Main marketing landing at `/`.
 * Implementation lives in `pages/home-v2` (images, videos, sections).
 * Legacy sections from `./Components/*` are unused here but kept in the repo for reference.
 */
export { default } from "../home-v2";
